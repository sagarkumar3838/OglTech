-- ============================================
-- RUN THIS NOW - Show me your exact columns
-- ============================================

-- This will show ALL your column names
SELECT * FROM practice_questions LIMIT 1;

-- ============================================
-- After running the above, you'll see columns like:
-- id | skill | level | ??? | ??? | ???
--
-- The ??? are what we need to know!
-- 
-- Common possibilities:
-- - question_text (instead of question)
-- - text (instead of question)
-- - q_text (instead of question)
-- - answer (instead of correct_answer)
-- - correct (instead of correct_answer)
-- ============================================
